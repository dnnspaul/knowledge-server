name: Release Please

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    # Only run if CI passed
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          release-type: node

      # Upload non-binary assets (platform-independent) when a release is created
      - uses: actions/checkout@v4
        if: ${{ steps.release.outputs.release_created == 'true' }}

      - name: Upload non-binary release assets
        if: ${{ steps.release.outputs.release_created == 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ steps.release.outputs.tag_name }}" \
            plugin/knowledge.ts \
            commands/consolidate.md \
            commands/knowledge-review.md \
            scripts/install.sh

  build-binaries:
    name: Build binaries (${{ matrix.os }})
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created == 'true' }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            suffix: linux-x64
          - os: macos-latest
            suffix: darwin-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build binaries
        run: |
          bun build --compile src/index.ts --outfile dist/knowledge-server-${{ matrix.suffix }}
          bun build --compile src/mcp/index.ts --outfile dist/knowledge-server-mcp-${{ matrix.suffix }}

      - name: Generate checksums
        run: |
          cd dist
          if command -v sha256sum &>/dev/null; then
            sha256sum knowledge-server-${{ matrix.suffix }} knowledge-server-mcp-${{ matrix.suffix }} > SHA256SUMS-${{ matrix.suffix }}
          else
            shasum -a 256 knowledge-server-${{ matrix.suffix }} knowledge-server-mcp-${{ matrix.suffix }} > SHA256SUMS-${{ matrix.suffix }}
          fi

      - name: Upload binaries and checksums
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ needs.release-please.outputs.tag_name }}" \
            dist/knowledge-server-${{ matrix.suffix }} \
            dist/knowledge-server-mcp-${{ matrix.suffix }} \
            dist/SHA256SUMS-${{ matrix.suffix }}
